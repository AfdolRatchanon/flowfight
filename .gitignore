# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase credentials — ห้าม commit เด็ดขาด
.env

# Firebase project pointer — project-specific, แต่ละคน config เอง
.firebaserc

# Firebase CLI cache — ไม่จำเป็น
.firebase

# firebase.json, firestore.rules → ควร commit (source code / config)
# .firebaserc → ไม่ commit, ใช้ .firebaserc.example เป็น template